if(APPLE)
  if(CMAKE_SYSTEM_PROCESSOR MATCHES "arm64")
    add_subdirectory(app/darwin-arm64 EXCLUDE_FROM_ALL)
  else()
    add_subdirectory(app/darwin-x64 EXCLUDE_FROM_ALL)
  endif()
endif()

if(WIN32)
  add_subdirectory(app/win32-x64 EXCLUDE_FROM_ALL)
endif()