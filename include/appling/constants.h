#ifndef APPLING_CONSTANTS_H
#define APPLING_CONSTANTS_H

#include "arch.h"
#include "os.h"

static const char *appling_os = APPLING_OS;

static const char *appling_arch = APPLING_ARCH;

static const char *appling_target = APPLING_TARGET;

static const char *appling_platform_dir = APPLING_PLATFORM_DIR;

static const char *appling_platform_entry = APPLING_PLATFORM_ENTRY;

static const char *appling_platform_current = APPLING_PLATFORM_CURRENT;

static const char *appling_platform_next = APPLING_PLATFORM_NEXT;

static const char *appling_platform_candidates[] = APPLING_PLATFORM_CANDIDATES;

#endif // APPLING_CONSTANTS_H
