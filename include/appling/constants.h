#ifndef APPLING_CONSTANTS_H
#define APPLING_CONSTANTS_H

#include <stddef.h>

#include "arch.h"
#include "os.h"

static const char *appling_os = APPLING_OS;

static const char *appling_arch = APPLING_ARCH;

static const char *appling_runtime = APPLING_RUNTIME;

static const char *appling_platform_dir = APPLING_PLATFORM_DIR;

static const char *appling_platform_bundle = APPLING_PLATFORM_BUNDLE;

static const char *appling_platform_exe = APPLING_PLATFORM_EXE;

static const char *appling_bin = APPLING_BIN;

static const char *appling_bin_next = APPLING_BIN_NEXT;

static const char *appling_bin_candidates[] = APPLING_BIN_CANDIDATES;

#endif // APPLING_CONSTANTS_H
